import 'package:flutter/material.dart';

class FillterViewBody extends StatelessWidget {
  const FillterViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [Center(child: Text("Fillter"))]);
  }
}
