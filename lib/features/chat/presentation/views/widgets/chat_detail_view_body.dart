import 'package:flutter/material.dart';

class ChatDetailViewBody extends StatelessWidget {
  const ChatDetailViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
